services:
  team-invite:
    build: .
    container_name: gpt-for-manage-invite
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
    volumes:
      - ./data:/app/data
      - ./database.json:/app/database.json
